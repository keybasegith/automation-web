node_modules
.next
.vercel
.env
.env.local
.env.production
.env.development
.DS_Store
*.log
tsconfig.tsbuildinfo

# Sensitive / private files
*.pdf
# Public event flyer served at /bike-fest — safe to publish
!public/bike-feast.pdf
*.docx
*.xlsx
uploads
documents
client-data
test-data

# Website ERP runtime content (edited via /admin, lives on the server's disk)
/data/executives.json
/data/cms
/data/cms-audit.json
/public/executives
/public/media

# Financial Statement Generator local store (contains financial figures)
.data/

# Financial Statement Generator: real client data stays out of this public repo.
# The Trial Balance fixture and the tests that assert its figures are kept
# locally only; the engine is deterministic, so the golden regression runs on a
# machine that has the file and is simply absent everywhere else.
lib/financial-statements/__fixtures__/
lib/financial-statements/__tests__/local/
